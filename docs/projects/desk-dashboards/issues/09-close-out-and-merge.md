# Close out and merge

Type: task
Status: open
Blocked by: 08
Repo: insights, erpnext

## Question

`/iris-review` loop on `feat/islands` until clean, prose check on commits and docs, history cleanup, ADRs for the decisions that pass the `docs/agents/domain.md` test, glossary updates in `CONTEXT.md`, then merge to `develop` and the ERPNext pull request. Effort docs are removed in the merge commit per `docs/agents/issue-tracker.md`.
