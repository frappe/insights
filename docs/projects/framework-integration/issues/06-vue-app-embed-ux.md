# Vue-frontend app embed UX

Type: grilling
Status: open
Blocked by: 04, 05

## Question

The second rendering environment: a CRM/HR-class app (own Vue frontend) mounts
Insights dashboards/charts as pages or blocks.

- What the app imports (from framework/ui? from Insights?) and how it mounts a
  dashboard/chart.
- Whether the desk base (ticket 02's mechanism, ticket 05's UX) transfers
  as-is — the one-base-two-environments goal — or the Vue path is a sibling.
- Replacing hand-rolled implementations (e.g. CRM's standard dashboard with its
  own permission-applying data provider) with the contract.

Deliberately last: desk first, one base proven, then this environment.
