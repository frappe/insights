Build, edit, explain or delete a Frappe Insights v3 workbook on a live site.

Read `SKILL_DIR/SKILL.md` and follow it exactly. It is the whole procedure. Read the
files under `SKILL_DIR/reference/` when SKILL.md tells you to.

The user's request: $ARGUMENTS
